/*
module: {
		rules: [
			{
				use: 'babel-loader',
				test: /\.js$/,
			},
			{
				use: ['style-loader', 'css-loader'],
				test: /\.css$/,
			},
		],
    },
    
    // loaders are applied right to left, so order is important. css-loader is applied first and its output is sent to style-loader
*/
